import styled from '@emotion/styled';

export const StatisticList = styled.ul`
  list-style: none;
  text-align: center;
`;

export const StatisticData = styled.span`
  margin: 10px 0;
  padding-right: 10px;
  font-size: 28px;
  font-weight: 500;
`;

export const StatisticResults = styled.span`
  margin: 10px 0;
  font-size: 28px;
  font-weight: 500;
`;

export const StatisticMessage = styled.p`
  margin: 10px 0;
  font-size: 28px;
  font-weight: 500;
`;
